@model Publishing

<div class="col-md-4 row pt-1 pb-1 mr-0 ml-0">
    <div class="card bg-light w-100">
        <div class="card-header">ID: @Model.Id</div>
        <div class="card-body">
            <h5 class="card-title">@Model.Name</h5>
            <p class="card-text">@Model.Description</p>
        </div>
        <div class="card-footer bg-transparent">
            <form asp-area="Admin" asp-controller="Publishing" asp-action="Delete" method="post">
                <a asp-area="Admin" asp-controller="Publishing" asp-action="Edit" class="btn btn-sm btn-warning" asp-route-publishingId=@Model.Id>Редактировать</a>
                <input type="hidden" name="PublishingId" value=@Model.Id />
                <button type="submit" class="btn btn-sm btn-danger">Удалить</button>
            </form>
        </div>
    </div>
</div>
